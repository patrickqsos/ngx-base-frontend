image: zaguee/ngcli:6.03

cache:
  paths:
    - node_modules

stages:
  - setup
  - lint
  - build
  
setup:
  stage: setup
  script:
    - npm install
  # cache:
  #   paths:
  #     - node_modules
 
lint:
  stage: lint
  script:
    - node --version
    #- ng --version
    - ng lint
  # cache:
  #   paths:
  #     - node_modules
 
build:
  stage: build
  script:
    - node --version
    #- ng --version
    - ng build --prod
  # cache:
  #   paths:
  #     - node_modules
  artifacts:
    paths:
      - dist