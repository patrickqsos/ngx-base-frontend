image: zaguee/base9

before_script:
  - tar -zxf /root/dev/mods.tar.gz

stages:
  - lint
  - build
  
lint:
  stage: lint
  script:
    - ng lint

build:
  stage: build
  script:
    - ng build --prod --no-progress
  artifacts:
    paths:
      - dist
